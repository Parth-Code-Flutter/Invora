name: pro_dialog
description: >-
  Centered, tone-colored Flutter dialogs with glow icons, entry motion,
  and outlined plus filled actions. Copy this folder into any Flutter app.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
